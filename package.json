{
  "name": "esbuild-js-app",
  "version": "1.0.0",
  "description": "A JS app with three entrypoints (a.js, b.js, c.js) that uses esbuild to bundle them",
  "main": "a.js",
  "scripts": {
    "build": "node esbuild/build.js",
    "serve": "serve ."
  },
  "dependencies": {
    "monaco-editor": "^0.39.0"
  },
  "devDependencies": {
    "esbuild": "^0.18.5",
    "serve": "^14.2.0"
  }
}
